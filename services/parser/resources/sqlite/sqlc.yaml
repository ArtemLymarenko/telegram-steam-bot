version: "2"
sql:
  - name: sqlite
    engine: "sqlite"
    queries: "query/query.sql"
    schema: "../migrations"
    gen:
      go:
        package: "sqlite"
        out: "../../internal/infrastructure/sqlite"
    database:
      uri: file:games?mode=memory&cache=shared
    rules:
      - sqlc/db-prepare